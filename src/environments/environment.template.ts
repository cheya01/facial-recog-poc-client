// Copy this file to environment.ts and environment.prod.ts
// Update the apiUrl with your actual backend URL

// For environment.ts (development):
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000'
};

// For environment.prod.ts (production):
// export const environment = {
//   production: true,
//   apiUrl: 'NG_APP_API_URL_PLACEHOLDER' // This will be replaced during build
// };
